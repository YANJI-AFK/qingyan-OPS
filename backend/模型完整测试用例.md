# 后端模型完整测试用例

> 测试目标：轻言OPS 数字人智能助手后端所有 API 接口、模型服务、对话流程
> 前置条件：后端已启动（`python app.py`），运行在 `http://127.0.0.1:5000`
> 测试工具：建议使用 Postman / curl / 浏览器地址栏 / 前端页面
> 文档中的 `{ }` 表示需要替换为实际值，`>` 表示 curl 命令

***


## 目录

1. [健康检查](#1-健康检查)
2. [对话意图测试](#2-对话意图测试)
3. [二次确认流程测试](#3-二次确认流程测试)
4. [工单 CRUD 测试](#4-工单-crud-测试)
5. [工单搜索测试](#5-工单搜索测试)
6. [统计看板测试](#6-统计看板测试)
7. [系统配置测试](#7-系统配置测试)
8. [人员管理测试](#8-人员管理测试)
9. [排班管理测试](#9-排班管理测试)
10. [角色管理测试](#10-角色管理测试)
11. [角色标签管理测试](#11-角色标签管理测试)
12. [监控数据测试](#12-监控数据测试)
13. [TTS 语音合成测试](#13-tts-语音合成测试)
14. [ASR 语音识别测试](#14-asr-语音识别测试)
15. [对话状态管理测试](#15-对话状态管理测试)
16. [LLM 意图理解专项测试](#16-llm-意图理解专项测试)
17. [边界错误测试](#17-边界错误测试)

***

## 1. 健康检查

### 1.1 基础状态检查

> curl <http://127.0.0.1:5000/>

| 编号    | 操作                             | 预期结果                                                                 | 实际结果   | 通过 |
| ----- | ------------------------------ | -------------------------------------------------------------------- | ------ | -- |
| 1.1.1 | 浏览器访问 `http://127.0.0.1:5000/` | 返回 JSON：`{"status":"ok","message":"数字人智能助手后端运行中","version":"1.0.0"}` | <br /> | □  |

### 1.2 不存在的路径

> curl <http://127.0.0.1:5000/not-exist-path>

| 编号    | 操作       | 预期结果                       | 实际结果   | 通过 |
| ----- | -------- | -------------------------- | ------ | -- |
| 1.2.1 | 访问不存在的路径 | 返回 404：`{"error":"接口不存在"}` | <br /> | □  |

***

## 2. 对话意图测试

### 2.1 问候

> curl -X POST <http://127.0.0.1:5000/chat> -H "Content-Type: application/json" -d "{"text":"你好"}"

| 编号    | 输入      | 预期结果                           | 实际结果   | 通过 |
| ----- | ------- | ------------------------------ | ------ | -- |
| 2.1.1 | `你好`    | 返回 `reply_text` 包含「轻言OPS 智能助手」 | <br /> | □  |
| 2.1.2 | `hello` | 同样返回问候语                        | <br /> | □  |

### 2.2 页面导航

> curl -X POST <http://127.0.0.1:5000/chat> -H "Content-Type: application/json" -d "{"text":"打开监控"}"

| 编号     | 输入        | 预期 `target`       | 预期 `action` | 通过 |
| ------ | --------- | ----------------- | ----------- | -- |
| 2.2.1  | `打开监控`    | `/monitor`        | `navigate`  | □  |
| 2.2.2  | `打开工单列表`  | `/tickets`        | `navigate`  | □  |
| 2.2.3  | `跳转到工单列表` | `/tickets`        | `navigate`  | □  |
| 2.2.4  | `打开统计看板`  | `/tickets/stats`  | `navigate`  | □  |
| 2.2.5  | `打开配置页`   | `/tickets/config` | `navigate`  | □  |
| 2.2.6  | `打开人员管理`  | `/staff`          | `navigate`  | □  |
| 2.2.7  | `查看人员列表`  | `/staff`          | `navigate`  | □  |
| 2.2.8  | `排班管理`    | `/staff/schedule` | `navigate`  | □  |
| 2.2.9  | `角色配置`    | `/staff/roles`    | `navigate`  | □  |
| 2.2.10 | `回到首页`    | `/`               | `navigate`  | □  |

### 2.3 工单查询（统计查询）

> curl -X POST <http://127.0.0.1:5000/chat> -H "Content-Type: application/json" -d "{"text":"有多少未完成的工单"}"

| 编号    | 输入           | 预期 `intent` / `api`           | 预期有返回数字    | 通过 |
| ----- | ------------ | ----------------------------- | ---------- | -- |
| 2.3.1 | `有多少未完成的工单`  | `query` / `tickets_stat`      | □          | □  |
| 2.3.2 | `有多少高优先级的工单` | `query` / `tickets_stat`      | □          | □  |
| 2.3.3 | `有多少工单`      | `query` / `tickets_stat`      | □          | □  |
| 2.3.4 | `有多少运维人员`    | `staff_query` / `staff_count` | 返回总人数和在职人数 | □  |
| 2.3.5 | `查人员数量`      | `staff_query` / `staff_count` | 同上         | □  |

### 2.4 工单详情查询（日期+序号）

> curl -X POST <http://127.0.0.1:5000/chat> -H "Content-Type: application/json" -d "{"text":"查看2026年7月27日的第一个工单"}"

| 编号    | 输入                   | 预期                          | 通过 |
| ----- | -------------------- | --------------------------- | -- |
| 2.4.1 | `查看2026年7月27日的第一个工单` | 返回 `TKT-20260727-0001` 工单详情 | □  |
| 2.4.2 | `查看7月28日的第三个工单`      | 返回 `TKT-20260728-0003` 工单详情 | □  |
| 2.4.3 | `看七月二十八日的第二个工单`      | 返回 `TKT-20260728-0002` 工单详情 | □  |
| 2.4.4 | `查看今天的第一个工单`         | 返回当天 `TKT-YYYYMMDD-0001` 工单 | □  |
| 2.4.5 | `看昨天第三个工单`           | 返回昨天 `TKT-YYYYMMDD-0003` 工单 | □  |

### 2.5 人员关联搜索

> curl -X POST <http://127.0.0.1:5000/chat> -H "Content-Type: application/json" -d "{"text":"张伟有多少高优先级的工单"}"

| 编号    | 输入             | 预期               | 通过 |
| ----- | -------------- | ---------------- | -- |
| 2.5.1 | `张伟有多少高优先级的工单` | 返回「张伟共有N个高优先级工单」 | □  |
| 2.5.2 | `王强有多少未完成的工单`  | 返回「王强共有N个未完成工单」  | □  |
| 2.5.3 | `李娜有多少工单`      | 返回「李娜共有N个工单」     | □  |
| 2.5.4 | `查数据库相关的工单`    | 返回关键词搜索结果        | □  |

### 2.6 监控查询

> curl -X POST <http://127.0.0.1:5000/chat> -H "Content-Type: application/json" -d "{"text":"CPU 使用率"}"

| 编号    | 输入        | 预期           | 通过 |
| ----- | --------- | ------------ | -- |
| 2.6.1 | `CPU 使用率` | 返回当前 CPU 百分比 | □  |
| 2.6.2 | `内存使用率`   | 返回当前内存百分比    | □  |
| 2.6.3 | `磁盘读写`    | 返回磁盘读写速率     | □  |
| 2.6.4 | `网络流量`    | 返回网络上行/下行    | □  |

### 2.7 趋势总结

> curl -X POST <http://127.0.0.1:5000/chat> -H "Content-Type: application/json" -d "{"text":"总结本月趋势"}"

| 编号    | 输入         | 预期       | 通过 |
| ----- | ---------- | -------- | -- |
| 2.7.1 | `总结本月趋势`   | 返回趋势分析报告 | □  |
| 2.7.2 | `总结本周工单趋势` | 返回本周趋势报告 | □  |

### 2.8 操作类（需二次确认）

> curl -X POST <http://127.0.0.1:5000/chat> -H "Content-Type: application/json" -d "{"text":"关闭工单TKT-20260727-0001"}"

| 编号    | 输入                          | 预期                               | 通过 |
| ----- | --------------------------- | -------------------------------- | -- |
| 2.8.1 | `关闭工单TKT-20260727-0001`     | 进入二次确认，`need_confirmation: true` | □  |
| 2.8.2 | `把工单TKT-20260727-0001指派给李四` | 进入二次确认                           | □  |
| 2.8.3 | `删除工单TKT-20260727-0001`     | 进入二次确认                           | □  |

### 2.9 未知意图

> curl -X POST <http://127.0.0.1:5000/chat> -H "Content-Type: application/json" -d "{"text":"今天天气怎么样"}"

| 编号    | 输入        | 预期               | 通过 |
| ----- | --------- | ---------------- | -- |
| 2.9.1 | `今天天气怎么样` | 返回兜底回复（引导用户正确提问） | □  |
| 2.9.2 | `讲个笑话`    | 返回兜底回复           | □  |

***

## 3. 二次确认流程测试

### 3.1 确认操作

> 先调用关闭工单 → 再调用确认

| 编号    | 步骤  | 操作                         | 预期                                  | 通过 |
| ----- | --- | -------------------------- | ----------------------------------- | -- |
| 3.1.1 | 第1步 | 发送 `关闭工单TKT-20260727-0001` | 返回 `need_confirmation: true`        | □  |
| 3.1.2 | 第2步 | 发送 `确认`                    | 工单状态更新成功，`need_confirmation: false` | □  |

### 3.2 取消操作

> 先调用关闭工单 → 再调用取消

| 编号    | 步骤  | 操作                         | 预期                               | 通过 |
| ----- | --- | -------------------------- | -------------------------------- | -- |
| 3.2.1 | 第1步 | 发送 `删除工单TKT-20260727-0001` | 进入二次确认                           | □  |
| 3.2.2 | 第2步 | 发送 `取消`                    | 返回已取消，`need_confirmation: false` | □  |

### 3.3 超时自动取消

| 编号    | 步骤  | 操作                         | 预期       | 通过 |
| ----- | --- | -------------------------- | -------- | -- |
| 3.3.1 | 第1步 | 发送 `关闭工单TKT-20260727-0001` | 进入二次确认   | □  |
| 3.3.2 | 第2步 | 等待 35 秒后，发送 `确认`           | 提示超时自动取消 | □  |

### 3.4 非确认输入

| 编号    | 步骤  | 操作                         | 预期             | 通过 |
| ----- | --- | -------------------------- | -------------- | -- |
| 3.4.1 | 第1步 | 发送 `关闭工单TKT-20260727-0001` | 进入二次确认         | □  |
| 3.4.2 | 第2步 | 发送 `随便`                    | 提示「请明确回答确认或取消」 | □  |

***

## 4. 工单 CRUD 测试

### 4.1 获取全部工单

> curl <http://127.0.0.1:5000/api/tickets>

| 编号    | 操作                 | 预期                                           | 通过 |
| ----- | ------------------ | -------------------------------------------- | -- |
| 4.1.1 | GET `/api/tickets` | 返回工单数组，每条含 id/title/status/priority/assignee | □  |

### 4.2 工单统计

> curl <http://127.0.0.1:5000/api/tickets/stat>

| 编号    | 操作                      | 预期                     | 通过 |
| ----- | ----------------------- | ---------------------- | -- |
| 4.2.1 | GET `/api/tickets/stat` | 返回统计对象（含总数、状态分布、优先级分布） | □  |

### 4.3 分页查询

> curl "<http://127.0.0.1:5000/api/tickets/page?page=1&limit=5>"

| 编号    | 操作                                        | 预期                                         | 通过 |
| ----- | ----------------------------------------- | ------------------------------------------ | -- |
| 4.3.1 | 分页：第1页，每页5条                               | 返回 `data` 含5条，`total` 为总数，`total_pages` 正确 | □  |
| 4.3.2 | 筛选：`?status=未完成`                          | 只返回状态为「未完成」的工单                             | □  |
| 4.3.3 | 筛选：`?priority=高`                          | 只返回高优先级工单                                  | □  |
| 4.3.4 | 筛选：`?assignee=张伟`                         | 只返回张伟负责的工单                                 | □  |
| 4.3.5 | 关键词：`?keyword=数据库`                        | 只返回标题含"数据库"的工单                             | □  |
| 4.3.6 | 组合筛选：`?status=未完成&priority=高&assignee=张伟` | 返回符合所有条件的工单                                | □  |
| 4.3.7 | 超出页码：`?page=9999`                         | 返回 `data: []`，`total` 不变                   | □  |

### 4.4 获取单个工单

> curl <http://127.0.0.1:5000/api/tickets/TKT-20260727-0001>

| 编号    | 操作       | 预期                              | 通过 |
| ----- | -------- | ------------------------------- | -- |
| 4.4.1 | 查看存在的工单  | 返回工单详情（含 description）           | □  |
| 4.4.2 | 查看不存在的工单 | 返回 404：`{"error":"工单 xxx 不存在"}` | □  |

### 4.5 新建工单

> curl -X POST <http://127.0.0.1:5000/api/tickets/create> -H "Content-Type: application/json" -d "{"title":"测试工单-请忽略","priority":"中","assignee":"张伟","description":"自动化测试创建的工单"}"

| 编号    | 操作          | 预期                          | 通过 |
| ----- | ----------- | --------------------------- | -- |
| 4.5.1 | 新建工单（完整参数）  | 返回 201，含 `ok: true` 和新工单 ID | □  |
| 4.5.2 | 缺少 title    | 返回 400：`工单标题不能为空`           | □  |
| 4.5.3 | 缺少 assignee | 返回 400：`负责人不能为空`            | □  |
| 4.5.4 | 空请求体        | 返回 400：`请求体不能为空`            | □  |

### 4.6 更新工单

> curl -X POST <http://127.0.0.1:5000/api/tickets/update> -H "Content-Type: application/json" -d "{"id":"TKT-20260727-0001","status":"已完成"}"

| 编号    | 操作         | 预期                             | 通过 |
| ----- | ---------- | ------------------------------ | -- |
| 4.6.1 | 更新状态       | 返回 `success: true`，含 `message` | □  |
| 4.6.2 | 更新优先级      | 返回 `success: true`             | □  |
| 4.6.3 | 同时更新状态+优先级 | 返回 `success: true`             | □  |
| 4.6.4 | 不存在的工单 ID  | 返回 400                         | □  |
| 4.6.5 | 缺少 id 参数   | 返回 400：`请提供 id`                | □  |

### 4.7 删除工单

> curl -X POST <http://127.0.0.1:5000/api/tickets/TKT-20260727-0001/delete>

| 编号    | 操作       | 预期                  | 通过 |
| ----- | -------- | ------------------- | -- |
| 4.7.1 | 删除未完成的工单 | 返回 `ok: true`       | □  |
| 4.7.2 | 删除已完成的工单 | 返回 400：`只能删除未完成的工单` | □  |
| 4.7.3 | 删除不存在的工单 | 返回 400：`工单不存在`      | □  |

### 4.8 工单历史

> curl <http://127.0.0.1:5000/api/tickets/TKT-20260727-0001/history>

| 编号    | 操作     | 预期                                     | 通过 |
| ----- | ------ | -------------------------------------- | -- |
| 4.8.1 | 查看工单历史 | 返回 `ok: true`，`data` 为数组，至少含1条「创建工单」记录 | □  |

### 4.9 指派候选人

> curl <http://127.0.0.1:5000/api/tickets/TKT-20260727-0001/candidates>

| 编号    | 操作      | 预期                         | 通过 |
| ----- | ------- | -------------------------- | -- |
| 4.9.1 | 获取可指派人员 | 返回 `ok: true`，`data` 为人员数组 | □  |

### 4.10 指派工单

> curl -X POST <http://127.0.0.1:5000/api/tickets/TKT-20260727-0001/assign> -H "Content-Type: application/json" -d "{"assignee":"李娜"}"

| 编号     | 操作          | 预期                       | 通过 |
| ------ | ----------- | ------------------------ | -- |
| 4.10.1 | 指派给启用人员     | 返回 `ok: true`            | □  |
| 4.10.2 | 缺少 assignee | 返回 400：`请提供 assignee 参数` | □  |
| 4.10.3 | 指派给不存在的姓名   | 返回 400                   | □  |

***

## 5. 工单搜索测试

### 5.1 多条件动态搜索

> curl -X POST <http://127.0.0.1:5000/api/tickets/search> -H "Content-Type: application/json" -d "{"assignee":"张伟","status":"未完成"}"
>
> <br />

| 编号    | 操作        | 预期                                     | 通过 |
| ----- | --------- | -------------------------------------- | -- |
| 5.1.1 | 按负责人+状态搜索 | 返回 `code: 200`，`data` 为匹配工单，`total` 正确 | □  |
| 5.1.2 | 按优先级搜索    | 返回高优先级工单                               | □  |
| 5.1.3 | 按关键词搜索    | 返回标题含关键词工单                             | □  |
| 5.1.4 | 组合4个条件    | 返回精确匹配结果                               | □  |
| 5.1.5 | 无匹配条件     | 返回 `data: []`，`total: 0`               | □  |
| 5.1.6 | 空请求体      | 返回所有工单                                 | □  |

***

## 6. 统计看板测试

### 6.1 看板数据

> curl "<http://127.0.0.1:5000/api/stats/dashboard?days=7>"

| 编号    | 操作               | 预期                 | 通过 |
| ----- | ---------------- | ------------------ | -- |
| 6.1.1 | 7天看板（默认）         | 返回含趋势、分布、SLA 等完整数据 | □  |
| 6.1.2 | 14天看板 `?days=14` | 返回 14 天范围的数据       | □  |
| 6.1.3 | 30天看板 `?days=30` | 返回 30 天范围的数据       | □  |
| 6.1.4 | 最小天数 `?days=0`   | 合法（自动限制为1天）        | □  |
| 6.1.5 | 最大天数 `?days=999` | 合法（自动限制为90天）       | □  |

***

## 7. 系统配置测试

### 7.1 读取配置

> curl <http://127.0.0.1:5000/api/config/params>

| 编号    | 操作       | 预期     | 通过 |
| ----- | -------- | ------ | -- |
| 7.1.1 | GET 读取配置 | 返回配置对象 | □  |

### 7.2 保存配置

> curl -X PUT <http://127.0.0.1:5000/api/config/params> -H "Content-Type: application/json" -d "{"sla\_timeout":"4","auto\_assign":"1"}"

| 编号    | 操作       | 预期                                 | 通过 |
| ----- | -------- | ---------------------------------- | -- |
| 7.2.1 | PUT 保存配置 | 返回 `success: true`，`updated` 为更新数量 | □  |
| 7.2.2 | 空请求体     | 返回 400：`请求体不能为空`                   | □  |

***

## 8. 人员管理测试

### 8.1 获取人员列表

> curl <http://127.0.0.1:5000/api/staff/list>

| 编号    | 操作                 | 预期                                              | 通过 |
| ----- | ------------------ | ----------------------------------------------- | -- |
| 8.1.1 | 全部人员               | 返回人员数组，含 staff\_no/name/role\_name/phone/status | □  |
| 8.1.2 | 关键词搜索 `?keyword=张` | 只返回姓名含"张"的人员                                    | □  |

### 8.2 新增人员

> curl -X POST <http://127.0.0.1:5000/api/staff> -H "Content-Type: application/json" -d "{"staff\_no":"OP099","name":"测试人员","role\_name":"运维工程师","phone":"13800000000"}"

| 编号    | 操作     | 预期                    | 通过 |
| ----- | ------ | --------------------- | -- |
| 8.2.1 | 新增完整信息 | 返回 `ok: true`，含新人员 ID | □  |
| 8.2.2 | 缺少必填字段 | 返回 400                | □  |
| 8.2.3 | 空请求体   | 返回 400：`请求体不能为空`      | □  |

### 8.3 编辑人员

> curl -X PUT <http://127.0.0.1:5000/api/staff/1> -H "Content-Type: application/json" -d "{"name":"更新后的名字"}"

| 编号    | 操作       | 预期            | 通过 |
| ----- | -------- | ------------- | -- |
| 8.3.1 | 编辑存在的 ID | 返回 `ok: true` | □  |
| 8.3.2 | 不存在的 ID  | 返回 400        | □  |
| 8.3.3 | 空请求体     | 返回 400        | □  |

### 8.4 重置密码

> curl -X POST <http://127.0.0.1:5000/api/staff/1/reset-password> -H "Content-Type: application/json" -d "{"password":"123456"}"

| 编号    | 操作                    | 预期            | 通过 |
| ----- | --------------------- | ------------- | -- |
| 8.4.1 | 重置密码（指定新密码）           | 返回 `ok: true` | □  |
| 8.4.2 | 重置密码（不指定，使用默认 123456） | 返回 `ok: true` | □  |

### 8.5 人员详情

> curl <http://127.0.0.1:5000/api/staff/1/detail>

| 编号    | 操作      | 预期                 | 通过 |
| ----- | ------- | ------------------ | -- |
| 8.5.1 | 获取存在的详情 | 返回人员详情 + 最近5条待处理工单 | □  |
| 8.5.2 | 不存在的 ID | 返回 404             | □  |

***

## 9. 排班管理测试

### 9.1 获取排班

> curl "<http://127.0.0.1:5000/api/staff/schedule?year=2026&month=7>"

| 编号    | 操作     | 预期                                                | 通过 |
| ----- | ------ | ------------------------------------------------- | -- |
| 9.1.1 | 获取当月排班 | 返回 `kpi` + `schedule` + `all_staff` + `load_data` | □  |

### 9.2 单条排班

> curl -X POST <http://127.0.0.1:5000/api/staff/schedule> -H "Content-Type: application/json" -d "{"staff\_name":"张伟","shift\_date":"2026-07-29","shift\_type":"早班"}"

| 编号    | 操作     | 预期            | 通过 |
| ----- | ------ | ------------- | -- |
| 9.2.1 | 保存有效排班 | 返回 `ok: true` | □  |
| 9.2.2 | 缺少必填字段 | 返回 400        | □  |
| 9.2.3 | 空请求体   | 返回 400        | □  |

### 9.3 删除排班

> curl -X POST <http://127.0.0.1:5000/api/staff/schedule/delete> -H "Content-Type: application/json" -d "{"staff\_name":"张伟","shift\_date":"2026-07-29"}"

| 编号    | 操作      | 预期                  | 通过 |
| ----- | ------- | ------------------- | -- |
| 9.3.1 | 删除存在的排班 | 返回 `ok: true`       | □  |
| 9.3.2 | 不存在的排班  | 返回 `ok: true`（幂等删除） | □  |

### 9.4 批量排班

> curl -X POST <http://127.0.0.1:5000/api/staff/schedule/batch> -H "Content-Type: application/json" -d "{"weekdays":\[1,3,5],"staff\_names":\["张伟","李娜"],"shift\_type":"早班","start\_date":"2026-07-01","end\_date":"2026-07-31"}"

| 编号    | 操作                  | 预期                            | 通过 |
| ----- | ------------------- | ----------------------------- | -- |
| 9.4.1 | 批量排班                | 返回 `ok: true`，`created` 为创建数量 | □  |
| 9.4.2 | 日期范围无效（end < start） | 返回 400                        | □  |
| 9.4.3 | 空请求体                | 返回 400                        | □  |

***

## 10. 角色管理测试

### 10.1 获取角色列表

> curl <http://127.0.0.1:5000/api/staff/roles>

| 编号     | 操作       | 预期                                             | 通过 |
| ------ | -------- | ---------------------------------------------- | -- |
| 10.1.1 | GET 角色列表 | 返回角色数组，含 role\_id/role\_name/tag\_id/tag\_name | □  |

### 10.2 新增角色

> curl -X POST <http://127.0.0.1:5000/api/staff/roles> -H "Content-Type: application/json" -d "{"role\_name":"测试角色"}"

| 编号     | 操作            | 预期                        | 通过 |
| ------ | ------------- | ------------------------- | -- |
| 10.2.1 | 新增角色          | 返回 `ok: true`，含新 role\_id | □  |
| 10.2.2 | 缺少 role\_name | 返回 400：`角色名不能为空`          | □  |

### 10.3 编辑角色

> curl -X PUT <http://127.0.0.1:5000/api/staff/roles> -H "Content-Type: application/json" -d "{"role\_id":1,"role\_name":"更新后的角色名"}"

| 编号     | 操作                       | 预期             | 通过 |
| ------ | ------------------------ | -------------- | -- |
| 10.3.1 | 编辑存在的角色                  | 返回 `ok: true`  | □  |
| 10.3.2 | 缺少 role\_id 或 role\_name | 返回 400：`参数不完整` | □  |

### 10.4 删除角色

> curl -X DELETE "<http://127.0.0.1:5000/api/staff/roles?role_id=999>"

| 编号     | 操作          | 预期                    | 通过 |
| ------ | ----------- | --------------------- | -- |
| 10.4.1 | 删除存在的角色     | 返回 `ok: true`         | □  |
| 10.4.2 | 缺少 role\_id | 返回 400：`role_id 不能为空` | □  |

***

## 11. 角色标签管理测试

### 11.1 获取标签列表

> curl <http://127.0.0.1:5000/api/staff/role-tags>

| 编号     | 操作       | 预期                                    | 通过 |
| ------ | -------- | ------------------------------------- | -- |
| 11.1.1 | GET 标签列表 | 返回标签数组，含 tag\_id/tag\_name/tag\_color | □  |

### 11.2 新增标签

> curl -X POST <http://127.0.0.1:5000/api/staff/role-tags> -H "Content-Type: application/json" -d "{"tag\_name":"测试标签","tag\_color":"#3b82f6"}"

| 编号     | 操作           | 预期                         | 通过 |
| ------ | ------------ | -------------------------- | -- |
| 11.2.1 | 新增标签（带颜色）    | 返回 `ok: true`，含新 tag\_id   | □  |
| 11.2.2 | 新增标签（不指定颜色）  | 返回 `ok: true`，默认颜色 #3b82f6 | □  |
| 11.2.3 | 缺少 tag\_name | 返回 400：`标签名不能为空`           | □  |

### 11.3 删除标签

> curl -X DELETE "<http://127.0.0.1:5000/api/staff/role-tags?tag_id=1>"

| 编号     | 操作         | 预期                   | 通过 |
| ------ | ---------- | -------------------- | -- |
| 11.3.1 | 删除存在的标签    | 返回 `ok: true`        | □  |
| 11.3.2 | 缺少 tag\_id | 返回 400：`tag_id 不能为空` | □  |

### 11.4 为角色设置标签

> curl -X POST <http://127.0.0.1:5000/api/staff/roles/tag> -H "Content-Type: application/json" -d "{"role\_id":1,"tag\_id":2}"

| 编号     | 操作              | 预期             | 通过 |
| ------ | --------------- | -------------- | -- |
| 11.4.1 | 设置角色标签          | 返回 `ok: true`  | □  |
| 11.4.2 | 取消标签（tag\_id=0） | 返回 `ok: true`  | □  |
| 11.4.3 | 缺少 role\_id     | 返回 400：`参数不完整` | □  |

***

## 12. 监控数据测试

### 12.1 最新监控数据

> curl <http://127.0.0.1:5000/api/servers/metrics>

| 编号     | 操作     | 预期                                       | 通过 |
| ------ | ------ | ---------------------------------------- | -- |
| 12.1.1 | 获取最新监控 | 返回 `metrics` 对象含 cpu/memory/disk/network | □  |

### 12.2 历史监控数据

> curl "<http://127.0.0.1:5000/api/servers/metrics/history?limit=20>"

| 编号     | 操作              | 预期                        | 通过 |
| ------ | --------------- | ------------------------- | -- |
| 12.2.1 | 获取历史趋势          | 返回 `data` 数组，每个元素含时间戳和指标值 | □  |
| 12.2.2 | 限制条数 `?limit=5` | 只返回5条                     | □  |

***

## 13. TTS 语音合成测试

### 13.1 获取语音列表

> curl <http://127.0.0.1:5000/api/tts/voices>

| 编号     | 操作     | 预期                      | 通过 |
| ------ | ------ | ----------------------- | -- |
| 13.1.1 | 获取可用语音 | 返回语音数组，含 id/name/gender | □  |

### 13.2 语音合成

> curl -X POST <http://127.0.0.1:5000/api/tts> -H "Content-Type: application/json" -d "{"text":"你好，欢迎使用轻言OPS系统"}" -o test\_tts.wav

| 编号     | 操作                 | 预期                    | 通过 |
| ------ | ------------------ | --------------------- | -- |
| 13.2.1 | 默认语音合成             | 返回 WAV 音频流，文件大小 > 1KB | □  |
| 13.2.2 | 指定语速 `{"rate": 5}` | 返回 WAV，语速较快           | □  |
| 13.2.3 | 空文本                | 返回 400                | □  |
| 13.2.4 | 长文本（500字）          | 成功返回 WAV              | □  |

***

## 14. ASR 语音识别测试

### 14.1 语音转写接口

> curl -X POST <http://127.0.0.1:5000/api/transcribe> -F "file=@test.wav"

| 编号     | 操作        | 预期                      | 通过 |
| ------ | --------- | ----------------------- | -- |
| 14.1.1 | 上传 WAV 文件 | 返回 `{"text": "识别出的文字"}` | □  |
| 14.1.2 | 未上传文件     | 返回 400：`请上传音频文件`        | □  |
| 14.1.3 | 空文件名      | 返回 400：`文件名为空`          | □  |

### 14.2 对话语音识别（base64）

> curl -X POST <http://127.0.0.1:5000/chat> -H "Content-Type: application/json" -d "{"audio\_data":"\<base64编码的音频>"}"

| 编号     | 操作           | 预期           | 通过 |
| ------ | ------------ | ------------ | -- |
| 14.2.1 | 提交 base64 音频 | 识别后执行意图并返回结果 | □  |
| 14.2.2 | 无效 base64    | 返回 500 或错误提示 | □  |

***

## 15. 对话状态管理测试

### 15.1 重置对话

> curl -X POST <http://127.0.0.1:5000/api/chat/reset>

| 编号     | 操作   | 预期                 | 通过 |
| ------ | ---- | ------------------ | -- |
| 15.1.1 | 重置对话 | 返回 `success: true` | □  |

### 15.2 调试状态

> curl <http://127.0.0.1:5000/api/chat/debug>

| 编号     | 操作    | 预期                             | 通过 |
| ------ | ----- | ------------------------------ | -- |
| 15.2.1 | 查看状态机 | 返回 state/context/history 等完整信息 | □  |

### 15.3 规则匹配调试

> curl -X POST <http://127.0.0.1:5000/api/chat/debug_rule> -H "Content-Type: application/json" -d "{"text":"打开监控"}"

| 编号     | 操作     | 预期                         | 通过 |
| ------ | ------ | -------------------------- | -- |
| 15.3.1 | 测试规则匹配 | 返回 `input` 和 `rule_result` | □  |

***

## 16. LLM 意图理解专项测试

> 这些测试验证 LLM 对用户输入的理解是否正确，需查看后端日志中的 `[意图]` 输出。

### 16.1 导航意图

| 编号     | 输入        | 期望 intent | 期望 api   | 通过 |
| ------ | --------- | --------- | -------- | -- |
| 16.1.1 | `打开监控`    | navigate  | navigate | □  |
| 16.1.2 | `跳转到工单列表` | navigate  | navigate | □  |
| 16.1.3 | `排班管理`    | navigate  | navigate | □  |
| 16.1.4 | `角色配置`    | navigate  | navigate | □  |

### 16.2 工单统计查询

| 编号     | 输入           | 期望 intent | 期望 api        | 通过 |
| ------ | ------------ | --------- | ------------- | -- |
| 16.2.1 | `有多少未完成的工单`  | query     | tickets\_stat | □  |
| 16.2.2 | `有多少高优先级的工单` | query     | tickets\_stat | □  |
| 16.2.3 | `最近一周完成了多少`  | query     | tickets\_stat | □  |

### 16.3 工单详情查询（日期+序号）

| 编号     | 输入                    | 期望 intent | 期望 params.id            | 通过 |
| ------ | --------------------- | --------- | ----------------------- | -- |
| 16.3.1 | `查看2026年7月27日的第一个工单`  | query     | `TKT-20260727-0001`     | □  |
| 16.3.2 | `查看二零二六年七月二十七日的第一个工单` | query     | `TKT-20260727-0001`     | □  |
| 16.3.3 | `查看七月二十七日的第三个工单`      | query     | `TKT-20260727-0003`     | □  |
| 16.3.4 | `看七月二十八日的第二个工单`       | query     | `TKT-20260728-0002`     | □  |
| 16.3.5 | `查看今天的第一个工单`          | query     | `TKT-YYYYMMDD-0001`（当天） | □  |
| 16.3.6 | `看昨天第三个工单`            | query     | `TKT-YYYYMMDD-0003`（昨天） | □  |

### 16.4 人员搜索

| 编号     | 输入             | 期望 intent | 期望 params                   | 通过 |
| ------ | -------------- | --------- | --------------------------- | -- |
| 16.4.1 | `张伟有多少高优先级的工单` | search    | `{assignee:张伟, priority:高}` | □  |
| 16.4.2 | `王强有多少未完成的工单`  | search    | `{assignee:王强, status:未完成}` | □  |
| 16.4.3 | `李娜有多少工单`      | search    | `{assignee:李娜}`             | □  |
| 16.4.4 | `查数据库相关的工单`    | search    | `{keyword:数据库}`             | □  |

### 16.5 人员统计

| 编号     | 输入        | 期望 intent    | 期望 api       | 通过 |
| ------ | --------- | ------------ | ------------ | -- |
| 16.5.1 | `有多少运维人员` | staff\_query | staff\_count | □  |
| 16.5.2 | `查人员数量`   | staff\_query | staff\_count | □  |

### 16.6 监控查询

| 编号     | 输入        | 期望 intent      | 期望 params         | 通过 |
| ------ | --------- | -------------- | ----------------- | -- |
| 16.6.1 | `CPU 使用率` | query\_monitor | `{target:cpu}`    | □  |
| 16.6.2 | `内存使用率`   | query\_monitor | `{target:memory}` | □  |

### 16.7 趋势总结

| 编号     | 输入         | 期望 intent | 期望 api          | 通过 |
| ------ | ---------- | --------- | --------------- | -- |
| 16.7.1 | `总结本月趋势`   | summary   | tickets\_report | □  |
| 16.7.2 | `总结本周工单趋势` | summary   | tickets\_report | □  |

### 16.8 操作类

| 编号     | 输入                          | 期望 intent | 期望 api          | 通过 |
| ------ | --------------------------- | --------- | --------------- | -- |
| 16.8.1 | `关闭工单TKT-20260727-0001`     | operate   | tickets\_update | □  |
| 16.8.2 | `删除工单TKT-20260727-0001`     | delete    | tickets\_delete | □  |
| 16.8.3 | `把工单TKT-20260727-0001指派给李四` | operate   | tickets\_assign | □  |

***

## 17. 边界错误测试

### 17.1 请求格式错误

| 编号     | 操作                                | 预期                             | 通过 |
| ------ | --------------------------------- | ------------------------------ | -- |
| 17.1.1 | POST `/chat` 发送空 JSON `{}`        | 返回 400：`请提供 text 或 audio_data` | □  |
| 17.1.2 | POST `/chat` 发送非法 JSON            | 返回 400                         | □  |
| 17.1.3 | POST `/api/tts` 发送空 body          | 返回 400                         | □  |
| 17.1.4 | PUT `/api/config/params` 发送空 body | 返回 400                         | □  |

### 17.2 参数越界

| 编号     | 操作                      | 预期               | 通过 |
| ------ | ----------------------- | ---------------- | -- |
| 17.2.1 | 分页参数 `?page=-1&limit=0` | 返回合法数据（自动修正为默认值） | □  |
| 17.2.2 | 看板 `?days=-1`           | 自动限制为1天          | □  |
| 17.2.3 | 看板 `?days=9999`         | 自动限制为90天         | □  |

### 17.3 并发测试

> 快速连续发送多个请求

| 编号     | 操作              | 预期              | 通过 |
| ------ | --------------- | --------------- | -- |
| 17.3.1 | 快速连续发送 10 次对话请求 | 全部返回 200，无连接池耗尽 | □  |
| 17.3.2 | 快速连续发送 10 次工单查询 | 全部返回正确数据        | □  |

***

## 测试结果汇总

| 模块         | 总用例数    | 通过     | 失败     | 通过率   |
| ---------- | ------- | ------ | ------ | ----- |
| 1. 健康检查    | 2       | <br /> | <br /> | %     |
| 2. 对话意图    | 25      | <br /> | <br /> | %     |
| 3. 二次确认    | 5       | <br /> | <br /> | %     |
| 4. 工单 CRUD | 20      | <br /> | <br /> | %     |
| 5. 工单搜索    | 6       | <br /> | <br /> | %     |
| 6. 统计看板    | 5       | <br /> | <br /> | %     |
| 7. 系统配置    | 2       | <br /> | <br /> | %     |
| 8. 人员管理    | 8       | <br /> | <br /> | %     |
| 9. 排班管理    | 8       | <br /> | <br /> | %     |
| 10. 角色管理   | 6       | <br /> | <br /> | %     |
| 11. 角色标签   | 7       | <br /> | <br /> | %     |
| 12. 监控数据   | 2       | <br /> | <br /> | %     |
| 13. TTS 合成 | 4       | <br /> | <br /> | %     |
| 14. ASR 识别 | 3       | <br /> | <br /> | %     |
| 15. 状态管理   | 3       | <br /> | <br /> | %     |
| 16. LLM 意图 | 20      | <br /> | <br /> | %     |
| 17. 边界错误   | 7       | <br /> | <br /> | %     |
| **总计**     | **133** | <br /> | <br /> | **%** |

***

> 测试日期：\_\_\_\_\_\_\_\_\_\_\_
> 测试人员：\_\_\_\_\_\_\_\_\_\_\_
> 备注：\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_

