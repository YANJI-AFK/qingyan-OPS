# 轻言OPS 后端依赖
# 安装方式: venv\Scripts\python.exe -m pip install -r backend\requirements.txt

# ===== Web 框架 =====
flask
flask-cors

# ===== 数据库（SQL Server 连接驱动）=====
pyodbc

# ===== 大模型调用（Ollama qwen3:8b）=====
requests

# ===== 语音识别 ASR（FunASR paraformer，全离线）=====
funasr
modelscope
numpy

# ===== 语音合成 TTS =====
# 引擎优先级: sherpa-onnx(离线) -> SAPI(离线, pywin32) -> edge-tts(在线,可选)
sherpa-onnx
pywin32
pyttsx3
edge-tts

# ===== 系统监控（实时 CPU/内存读取）=====
psutil
